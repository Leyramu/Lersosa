# Tomcat
server:
  port: 8718

# Spring
spring:
  application:
    # 应用名称
    name: @pom.artifactId@
  profiles:
    # 环境配置
    active: @PROFILE-ACTIVE@
  main:
    lazy-initialization: true

sentinel:
  dashboard:
    version: @sentinel.version@

